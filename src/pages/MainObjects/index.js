import React from 'react';
import {motion} from 'framer-motion/dist/framer-motion'

function MainObjects() {

    return (
        <motion.div 
        id="landingPage" animate={{backgroundColor: 'hsl(0, 0, 0)', color: 'hsl(256, 256, 256)'}}>
        testin
        </motion.div>
    )
}

export default MainObjects;